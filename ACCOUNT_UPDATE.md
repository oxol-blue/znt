# 账号更新说明

## 更新内容

将系统登录账号从自定义用户名改为：

- **学生端**：使用学号登录（如：2021001000）
- **教师端**：使用工号登录（如：T001）
- **密码规则**：与账号相同

## 测试账号

### 学生账号

| 学号         | 姓名  | <br /> | 专业       | 密码         |
| ---------- | --- | :----- | -------- | ---------- |
| 2021001000 | 张同学 | <br /> | 计算机科学与技术 | 2021001000 |
| 2021001001 | 刘同学 | <br /> | 软件工程     | 2021001001 |
| 2021001002 | 陈同学 | <br /> | 计算机科学与技术 | 2021001002 |
| 2021001003 | 赵同学 | <br /> | 信息安全     | 2021001003 |

### 教师账号

| 工号   | 姓名  | 院系         | 密码   |
| ---- | --- | ---------- | ---- |
| T001 | 王教授 | 计算机科学与技术学院 | T001 |
| T002 | 李讲师 | 软件工程学院     | T002 |

## 如何应用更新

### 方法一：重新初始化数据库（推荐用于开发环境）

```bash
# 1. 删除现有数据库（慎用！会清空所有数据）
mysql -u root -p -e "DROP DATABASE IF EXISTS znt;"

# 2. 重新创建数据库
mysql -u root -p -e "CREATE DATABASE znt CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 3. 运行新的初始化脚本
mysql -u root -p znt < database/init.sql
```

### 方法二：更新现有数据库（保留数据）

```bash
# 运行更新脚本
mysql -u root -p znt < database/update_accounts.sql
```

### 方法三：手动更新（针对特定用户）

如果需要生成新的bcrypt密码哈希：

```python
import bcrypt

account = '2021001000'  # 学号或工号
salt = bcrypt.gensalt(rounds=12)
hashed = bcrypt.hashpw(account.encode('utf-8'), salt)
print(hashed.decode('utf-8'))
```

然后在数据库中执行：

```sql
UPDATE users 
SET username = '2021001000', 
    password = '$2b$12$...生成的哈希...'
WHERE id = 1;
```

## 前端显示更新

登录页面已更新显示新的测试账号信息：

- 学生：2021001000 \~ 2021001003
- 教师：T001 / T002

## 重启服务

更新数据库后，请重启后端服务：

```bash
cd backend
python app.py
```

## 验证登录

1. 打开登录页面 <http://localhost:5000>
2. 使用学号/工号登录
3. 密码与账号相同

## 注意事项

1. **数据备份**：在生产环境执行前务必备份数据库
2. **bcrypt哈希**：密码使用bcrypt算法加密，每次生成的哈希值不同但都能验证
3. **关联数据**：学生和教师表的关联关系保持不变（通过user\_id关联）
4. **AI助手**：AI助手现在可以根据学号/工号查询学生/教师的个人数据

